#include <iostream>
using namespace std;

int main(){
    int number;
    cout<<"ENTER THE NUMBER : ";
    cin>>number;

    if(number%2==0){

         cout<<"It is even number"<<"\n"; // if condition just write if(condition) and below what to do
         cout<<"Because it is divisible by 2"<<"\n";
    }

    else if(number%2!=0){

        cout<<"It is odd number";// same like c language
    return 0;
}